{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Triage",
  "type": "object",
  "additionalProperties": false,
  "required": ["classifications"],
  "properties": {
    "classifications": {
      "type": "array",
      "description": "One classification per changed file in the PR.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["path", "class"],
        "properties": {
          "path": {
            "type": "string",
            "description": "Repo-relative path of the changed file."
          },
          "class": {
            "type": "string",
            "enum": ["trivial", "formatting", "doc", "simple", "complex"],
            "description": "trivial = autogenerated/lockfile-style; formatting = whitespace/style only; doc = docs/comments only; simple = small targeted change; complex = needs deep review."
          }
        }
      }
    }
  }
}
