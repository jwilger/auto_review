{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PreMergeCustom",
  "type": "object",
  "additionalProperties": false,
  "required": ["checks"],
  "properties": {
    "checks": {
      "type": "array",
      "description": "One result per repo-author-supplied check, in input order.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["status", "rationale"],
        "properties": {
          "status": {
            "type": "string",
            "enum": ["pass", "fail", "skip"],
            "description": "pass = the check holds in this diff; fail = it doesn't; skip = the diff doesn't touch anything the check could apply to."
          },
          "rationale": {
            "type": "string",
            "description": "One-sentence reason. Surfaced verbatim under the checklist item — keep it concrete and avoid vague phrasing."
          }
        }
      }
    }
  }
}
