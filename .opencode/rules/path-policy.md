# Repository-local shell path policy

For repository-local shell work, use project-local relative paths (for example `.`, `./path`, repo-root-relative paths like `.opencode/...`, or `git diff`), and avoid absolute paths under a home-directory path for project-local operations.
